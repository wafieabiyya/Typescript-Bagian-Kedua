let jobName: string [] = ["Programmer","Footballer","Businessman"] //declare array value

for (let counter = 0; counter<jobName.length; counter++){ //looping for array
    console.log(jobName[counter])
}